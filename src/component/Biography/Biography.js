import ImageContainer from "../ImageContainer";
import BottomContent from "../Footer/BottomContent";

const Biography = () => {
    return (
      <div>
        <ImageContainer page="Biography"/>
        <BottomContent />
      </div>
    );
  };

export default Biography;
